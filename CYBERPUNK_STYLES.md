# Киберпанк UI - Система уведомлений и стили

## Обновления

### 1. Система уведомлений
- **Store**: `src/stores/notifications.ts` - Pinia store для управления уведомлениями
- **Компонент**: `src/components/NotificationContainer.vue` - UI компонент для отображения уведомлений
- **Использование**: Импортируйте `useNotificationsStore` в любом компоненте и вызывайте методы:
  ```typescript
  const notificationsStore = useNotificationsStore()
  notificationsStore.addNotification('Текст', 'success', 3000) // type: success | error | warning | info
  ```

### 2. Киберпанк CSS

#### Цветовая палитра (переменные в base.css)
- `--cp-primary`: #00ffff (Основной цвет - голубой)
- `--cp-secondary`: #ff006e (Вторичный - розовый)
- `--cp-accent`: #00ff41 (Акцент - зеленый)
- `--cp-warning`: #ffbe0b (Предупреждение - желтый)
- `--cp-danger`: #ff006e (Опасность - красный)
- `--cp-success`: #00ff41 (Успех - зеленый)

#### Структура стилей
- Темный фон с градиентом (#050812 → #0a0e27)
- Карточки с backdrop-filter blur и glowing эффектами
- Кнопки с киберпанк стилем (границы, свечение)
- Плавные переходы и анимации

### 3. Где добавить фоновые изображения

Замените комментарии `/* ФОНОВОЕ ИЗОБРАЖЕНИЕ: ... */` на реальные URL:

1. **ConditionalListDemo.vue** (строка ~7):
   ```css
   background: linear-gradient(...), url('YOUR_IMAGE_URL');
   background-size: cover;
   ```

2. **PaletteGenerator.vue** (строка ~8):
   - Для киберпанк эффекта используйте текстуры с нео-неоном

3. **ReactiveDemo.vue** (строка ~8):
   - Абстрактные киберпанк паттерны или сетки

### 4. Компоненты с обновленными стилями

✓ **App.vue** - Киберпанк навигация и шаг
✓ **ConditionalListDemo.vue** - Списки с киберпанк стилем + уведомления
✓ **HomePage.vue** - Главная страница с новыми стилями
✓ **NotFound.vue** - 404 страница
✓ **PaletteGenerator.vue** - Генератор палитр (полностью переработан)
✓ **ReactiveDemo.vue** - Демо реактивности

### 5. Типы уведомлений

- `success` - Зеленое свечение (#00ff41)
- `error` - Красное свечение (#ff006e)
- `warning` - Желтое свечение (#ffbe0b)
- `info` - Голубое свечение (#00ffff)

### 6. Примеры использования

```typescript
// В любом компоненте
import { useNotificationsStore } from '@/stores/notifications'

const notificationsStore = useNotificationsStore()

// Успешное действие
notificationsStore.addNotification('Данные сохранены', 'success')

// Ошибка
notificationsStore.addNotification('Ошибка при загрузке', 'error', 4000)

// Предупреждение
notificationsStore.addNotification('Заполните все поля', 'warning')

// Информация
notificationsStore.addNotification('Загрузка...', 'info', 0) // 0 = не закроется автоматически
```

## Заметки для дизайнера

- Все кнопки используют шрифт 'Courier New' для киберпанк эффекта
- Текст в верхнем регистре с увеличенным letter-spacing
- Glowing эффекты создаются через box-shadow с rgba цветов
- Используется backdrop-filter: blur для стеклянного эффекта
- Все заголовки имеют text-shadow для свечения

## Совместимость

- Chrome/Edge: ✓ (полная поддержка)
- Firefox: ✓ (полная поддержка)
- Safari: ⚠ (backdrop-filter работает, но может требовать -webkit префикса)
- IE11: ✗ (не поддерживается)

Стили готовы к использованию! 🎮✨
